def disableDebugBuild() {
//    def targetTasks = project.tasks.findAll { task ->
//        task.name.contains("Debug")
//    }
//    targetTasks.each {
//        println "disable debug task  :${it.name}"
//        it.setEnabled false
//    }
}

ext {
    spCompileOptions = [
            compileSdk: 25,
            buildTools: "25.0.3",
            minSdk    : 19,
            targetSdk : 25
    ]

    spSdk = [
            name  : ':sdk',
            jarDir: 'src/jarLibs',
            aarDir: 'src/aarLibs',
            jniDir: 'src/jniLibs/armeabi'
    ]

    taskDisableDebugBuild = this.&disableDebugBuild
}
